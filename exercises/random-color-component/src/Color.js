import React from "react"

function Color (props) {
    console.log(props.colors)

    return (
        <div>
        {/* <h2>{props.colors.color}</h2>
        <img width='400' src={props.colors.image} alt=''/> */}
        </div>
    )
}
export default Color 